// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#3c3c3c',
    },
    // other customization...
  },
});

export default theme;
